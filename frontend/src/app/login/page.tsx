"use client";
import {useState} from "react";import {ArrowRight,ShieldCheck} from "lucide-react";import "./login.css";
const BASE=process.env.NEXT_PUBLIC_API_BASE_URL||"http://localhost:8000/api/v1";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch(`${BASE}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(!r.ok)throw new Error(r.status===401?"Email or password is incorrect":"Authentication service unavailable");const data=await r.json();localStorage.setItem("pathsense_token",data.access_token);location.href="/"}catch(e){setError(e instanceof Error?e.message:"Sign in failed")}finally{setLoading(false)}}return <main className="loginPage"><section className="loginIdentity"><div className="brand"><div className="brandmark"><ShieldCheck size={21}/></div><div><strong>PathSense</strong><span>LOGISTICS INTELLIGENCE</span></div></div><div><span className="eyebrow">ACCESSIBILITY BEFORE DISPATCH</span><h1>Smarter logistics starts with a route you can trust.</h1><p>Understand weather, terrain and operational risk across North-Eastern India before cargo leaves the depot.</p></div><small>Shortest ≠ Most accessible · Navigation → Accessibility Intelligence</small></section><section className="loginForm"><form onSubmit={submit}><span className="eyebrow">SECURE OPERATOR ACCESS</span><h2>Welcome back</h2><p>Sign in to access route history, deliveries and reports.</p><label>Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="operator@company.com" required/></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/></label>{error&&<div className="loginError" role="alert">{error}</div>}<button className="analyze" disabled={loading}>{loading?"Signing in…":<>Sign in <ArrowRight size={17}/></>}</button></form></section></main>}
